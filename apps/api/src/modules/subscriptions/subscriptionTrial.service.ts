class SubscriptionTrialService {
  async checkTrialExpirations(): Promise<void> {
    // Scaffold: Find subscriptions in trial, check expiration, transition status
    // Integrate with Plan 8 for email reminders
  }
}

export default new SubscriptionTrialService();
